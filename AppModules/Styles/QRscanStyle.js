import { StyleSheet } from "react-native";

export const QRScanStyles = StyleSheet.create({
    textStyle: {
        fontSize: 20,
        color: "rgb(0,125,0)"
    },
    container: { marginTop: 57 }
});